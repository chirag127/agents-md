# Cline rules — Chirag Singhal

> **Canonical rules:** see [`~/AGENTS.md`](file://~/AGENTS.md) (or [github.com/chirag127/agents-md](https://github.com/chirag127/agents-md)).
> Edit only the canonical file; this stub is generated and overwritten on every sync.

## Cline-specific quirks

- **`.clinerules` (file)** OR **`.clinerules/` (dir of files)** — both accepted. Stub mode uses the file form.
- **Plan mode vs Act mode:** Cline distinguishes; my preference is "propose, wait, then act" — see `~/AGENTS.md` "How you (the agent) update these rules."
